//- {fields: [html], desc: html}
//- '[ngFormModel]'='form'
form(
  '#my_form'='ngForm'
  '(ngSubmit)'='onSubmit()'
)
  .fn_desc!= desc
  - each fld in fields
    != fld
  button.btn.waves-effect.waves-light(
    type='submit'
    [disabled]='!my_form.form.valid'
  ) Submit
