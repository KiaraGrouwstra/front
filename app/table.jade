//- use ngAnimate with ngFor when the latter is out for ng? that yoyo animation looks spiffy.
table
  thead
    th(*ng-for="#col of cols") {{col}}
  tbody
    tr(*ng-for="#row of rows")
      td(*ng-for="#col of cols") {{row[col]}}
