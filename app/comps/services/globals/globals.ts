// import { Injectable } from '@angular/core';

// exposes some utilities for use in ng2 views

// @Injectable()
export class GlobalsService {
  _ = require('lodash/fp');
  // lodash = require('lodash');
  // R = require('ramda');
  R = require('ramda-maybe');
  Object = Object;
  Array = Array;
  JSON = JSON;
  console = console;

  // constructor() {}

}
