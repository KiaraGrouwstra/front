.switch
  label
    | Off
    input(
      type='checkbox'
      '[ngClass]'=`{ valid: ctrl.valid, invalid: !ctrl.valid }`
      '[formControl]'=`ctrl`
      '[(ngModel)]'=`ctrl.value`
      '[setAttrs]'=`attrs`
    )
    span.lever
    | On
