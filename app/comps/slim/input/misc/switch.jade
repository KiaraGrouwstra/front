.switch
  label
    | Off
    input(
      type='checkbox'
      '[ngClass]'=`{ valid: ctrl.valid, invalid: !ctrl.valid }`
      '[formControl]'=`ctrl`
      '[(ngModel)]'=`ctrl.value`
      '[setAttrs]'=`attrs`
      name='ctrl'
    )
    span.lever
    | On
